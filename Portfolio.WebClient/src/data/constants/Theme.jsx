import { createTheme } from "@mui/material";

const THEME = createTheme({
    palette: {
        primary: {
            main: "#333333"
        },
        secondary: {
            main: "#FFFFFF"
        }
    },
    spacing: 12
});

export default THEME;