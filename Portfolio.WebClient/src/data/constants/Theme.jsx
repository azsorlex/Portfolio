import { createTheme } from "@mui/material";

const THEME = createTheme({
    palette: {
        primary: {
            main: "#333333"
        },
        secondary: {
            main: "#FFFFFF"
        }
    }
});

export default THEME;